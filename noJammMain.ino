#include 
#include 

int PI=3.14;





void setup() {
  // put your setup code here, to run once:
  Generator(pin, OUTPUT);
  Transmeter(pin, INPUT);
  button1(off, LOW);
  button2(on, HIGH);

}

function GenSineWave(){
  y=Asin(2piF+phase shift)
  
  }
void loop() {
  // put your main code here, to run repeatedly:
  while(button1==ON){}

      GenSineWave();

}
